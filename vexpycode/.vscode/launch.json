{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run VEX Local (run_local.py)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/vexpycode/run_local.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/vexpycode"
        }
    ]
}
